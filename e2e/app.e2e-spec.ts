import { AppPage } from './app.po';

describe('fk-marketplace-frontend App', () => {
  let page: AppPage;

  beforeEach( () => {
    page = new AppPage();
  });

  it('should display welcome message', () => {
      // console.log(page);
      // page.navigateTo();
      // console.log(page.getParagraphText());
      // expect(page.getParagraphText()).toEqual('PLEASE LOG IN');
  });
});
