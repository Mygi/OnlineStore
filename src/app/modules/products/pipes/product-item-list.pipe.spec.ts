import { ProductItemListSortPipe } from './product-item-list.pipe';

describe('ProductItemListSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductItemListSortPipe();
    expect(pipe).toBeTruthy();
  });
});
